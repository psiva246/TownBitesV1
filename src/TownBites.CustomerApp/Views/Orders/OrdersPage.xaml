<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="TownBites.CustomerApp.Views.Orders.OrdersPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="My Orders">

    <RefreshView
        Command="{Binding LoadOrdersCommand}"
        IsRefreshing="{Binding IsBusy}">

        <CollectionView ItemsSource="{Binding Orders}">

            <CollectionView.ItemTemplate>

                <DataTemplate>

                    <Frame Margin="10" Padding="10">

                        <Frame.GestureRecognizers>

                            <TapGestureRecognizer
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}},
                                                  Path=BindingContext.OpenOrderCommand}"
                                CommandParameter="{Binding}" />

                        </Frame.GestureRecognizers>

                        <VerticalStackLayout>

                            <Label
                                Text="{Binding OrderNumber}"
                                FontAttributes="Bold"
                                FontSize="18"/>

                            <Label
                                Text="{Binding RestaurantName}"/>

                            <Label
                                Text="{Binding Status}"
                                TextColor="Green"/>

                            <Label
                                Text="{Binding TotalAmount, StringFormat='₹{0:F2}'}"/>

                            <Label
                                Text="{Binding OrderedOn, StringFormat='{0:dd MMM yyyy HH:mm}'}"/>

                        </VerticalStackLayout>

                    </Frame>

                </DataTemplate>

            </CollectionView.ItemTemplate>

        </CollectionView>

    </RefreshView>

</ContentPage>